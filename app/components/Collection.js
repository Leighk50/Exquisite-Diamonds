import ProductCard from "./ProductCard";import {byCategory} from "../data/products";export default function Collection({category,title,intro}){return <main><section className="pageHero"><p className="eyebrow">EXQUISITE DIAMONDS</p><h1>{title}</h1><p>{intro}</p></section><section className="collectionGrid">{byCategory(category).map(p=><ProductCard key={p.slug} product={p}/>)}</section></main>}