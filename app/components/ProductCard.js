import Link from "next/link";import {money} from "../data/products";export default function ProductCard({product}){return <article className="productCard">{product.justArrived&&<span className="newTag">JUST ARRIVED</span>}<Link href={"/product/"+product.slug} className="productImage"><span>◇</span></Link><h4>{product.name}</h4><p>{product.shape} · {product.metal}</p><strong>{money(product.price)}</strong><Link className="cardButton" href={"/product/"+product.slug}>VIEW DETAILS</Link></article>}