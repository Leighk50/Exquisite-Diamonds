import {notFound} from "next/navigation";import {products,money} from "../../data/products";import AddToBasket from "../../components/AddToBasket";export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function Page({params}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main className="productPage"><div className={"gallery realisticGallery "+p.category}><div className="largeJewel">{p.category==="rings"?"💍":p.category==="earrings"?"◈  ◈":"◇"}</div><small>Demo representation — replace with photography of the actual piece</small></div><div className="productInfo">{p.justArrived&&<p className="eyebrow">JUST ARRIVED</p>}<h1>{p.name}</h1><p className="productPrice">{money(p.price)}</p><p className="description">{p.description}</p><p><b>Period:</b> {p.period||"Vintage"}</p><p><b>Metal:</b> {p.metal}</p><AddToBasket product={p}/><p className="delivery">Complimentary insured UK delivery · Luxury presentation</p><h2>Diamond & Piece Details</h2><div className="fiveCs">{[["CARAT",p.carat],["CUT",p.cut],["COLOUR",p.colour],["CLARITY",p.clarity],["DOCUMENTATION",p.certLab]].map(x=><div key={x[0]}><span>{x[0]}</span><b>{x[1]}</b></div>)}</div><div className="certificate"><h3>Provenance & Documentation</h3><p>{p.certificate}</p><small>Demo catalogue information. Actual pieces will show known age, hallmarks, provenance, valuation and certification where available.</small></div></div></main>}